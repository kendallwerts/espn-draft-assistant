[tool.poetry]
package-mode = false

[project]
name = "espn-draft-assistant"
version = "0.1.0"
description = "Live ESPN fantasy football draft assistant"
readme = "README.md"
requires-python = ">=3.11,<4.0"
dependencies = [
  "requests>=2.31,<3",
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
